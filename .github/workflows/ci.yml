on: push

name: CI
jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: cashapp/activate-hermit@v1
      - name: Test
        run: |
          go test -v ./...
